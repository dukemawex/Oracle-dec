name: OracleDeck Forecasting Bot

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: bot
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run OracleDeck bot
        working-directory: bot
        env:
          METACULUS_TOKEN: ${{ secrets.METACULUS_TOKEN }}
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.AGENTROUTER_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.AGENTROUTER_API_KEY }}
          ANTHROPIC_BASE_URL: https://agentrouter.org/
          OPENAI_API_KEY: ${{ secrets.AGENTROUTER_API_KEY }}
          OPENAI_BASE_URL: https://agentrouter.org/v1
          TINYFISH_API_KEY: ${{ secrets.TINYFISH_API_KEY }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          BACKEND_URL: ${{ secrets.BACKEND_URL }}
          METACULUS_API_BASE: https://shjrjrjds.metaculus.com/api2
        run: PYTHONPATH=src python -m oracledeck_bot.main
